git status
git merge origin/master --allow-unrelated-histories
git pull origin master //do powiazania branch z master aby nie wpisywac -upstream
git push --set-upstream origin master
git commit -m "command"
git add -A